---
import { basics } from "@resume";
const { aboutMe } = basics;
---

<section class="flex flex-col gap-2 mt-5">
  <h1 class="text-xl font-semibold">👋 About me</h1>
  <p class="text-sm" set:html={aboutMe} />
</section>
